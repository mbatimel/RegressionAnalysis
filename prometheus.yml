global:
  scrape_interval: 3s

scrape_configs:
  - job_name: "prometheus"
    scrape_interval: "3s"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "go_service_metrics"
    scrape_interval: "3s"
    static_configs:
      - targets: ["docker.for.mac.localhost:9093"]
